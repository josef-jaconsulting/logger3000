---
description: "Recurring questions and answers. Append-only, entries tagged for scanning."
updated: <YYYY-MM-DD>
max_lines: 200
---

# FAQ

*Add an entry whenever the same question gets asked (by a human or an
agent) more than once. If you find yourself re-explaining something,
it belongs here instead of in someone's memory. Each entry carries its
own `tags:` line so an agent can scan just the questions + tags before
reading full answers — see README.md for the scanning convention.*

### Q: <question>
tags: [<topic>, <topic>]
A: <answer> (see also: link to systemPatterns.md / techContext.md if relevant)

---

<!-- Template for new entries above this line. If this file exceeds
     max_lines, archive resolved/stale entries into faq-archive.md rather
     than deleting them outright. -->
